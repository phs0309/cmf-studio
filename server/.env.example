# Server Configuration
PORT=10000
NODE_ENV=production

# Client Configuration (for CORS) - Will be set automatically by Render
CLIENT_URL=https://cmf-studio-frontend.onrender.com

# Database Configuration
# DATABASE_URL will be provided by Render if using PostgreSQL
# For now using SQLite with /tmp storage

# File Upload Configuration
MAX_FILE_SIZE=10485760
UPLOAD_DIR=/tmp/uploads